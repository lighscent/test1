// samples/js/utils.js
// Helper functions for the JavaScript app.

function greet(name) {
    return `Hello, ${name}! This is the JavaScript sample app.`;
}

function add(a, b) {
    return a + b;
}

module.exports = { greet, add };
